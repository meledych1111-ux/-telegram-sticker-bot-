{
  "scripts": {
    "setup": "node setup-webhook.js",
    "webhook:info": "node -e \"require('./setup-webhook.js').getWebhookInfo()\"",
    "webhook:delete": "node -e \"require('./setup-webhook.js').deleteWebhook()\"",
    "neon:test": "node -e \"require('./setup-webhook.js').testNeonConnection()\"",
    "env:check": "node -e \"const check = (name) => console.log(name + ':', process.env[name] ? '✓' : '✗'); ['TELEGRAM_BOT_TOKEN', 'VERCEL_URL', 'DATABASE_URL', 'PGHOST_UNPOOLED'].forEach(check); console.log('\\nNeon Project:', process.env.NEON_PROJECT_ID || 'Not set')\""
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0"
  }
}
