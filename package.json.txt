{
  "name": "telegram-sticker-bot",
  "version": "1.0.0",
  "description": "Telegram bot for creating stickers with effects, collections and favorites",
  "main": "api/bot.js",
  "scripts": {
    "dev": "vercel dev",
    "setup-webhook": "node scripts/setupWebhook.js",
    "update-db": "node scripts/updateDatabase.js",
    "deploy": "vercel --prod",
    "postinstall": "npm rebuild sharp --platform=linux --arch=x64"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "sharp": "^0.33.4",  // ← ОБНОВЛЕНО ДО ПОСЛЕДНЕЙ ВЕРСИИ
    "postgres": "^3.4.3",
    "form-data": "^4.0.0"
  },
  "engines": {
   "node": "18.x || 20.x || 22.x || 24.x"
  }
}
