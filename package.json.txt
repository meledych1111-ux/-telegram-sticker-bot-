{
  "name": "sticker-bot",
  "version": "1.0.0",
  "engines": {
    "node": "18.x || 20.x || 22.x || 24.x"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  },
  "scripts": {
    "postinstall": "npm rebuild sharp --platform=linux --arch=x64 --libc=glibc"
  }
}
